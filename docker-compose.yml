version: "3.3"
services: 
  db:
    container_name: postgres
    image: postgres
    ports:
      - "5432:5432"
    environment: 
      - POSTGRES_PASSWORD=1234  
    volumes: 
      - "./db:/var/lib/postgresql/data"
